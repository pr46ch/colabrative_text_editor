# Public URL of the separately deployed HTTP API. Do not include a trailing slash.
NEXT_PUBLIC_API_BASE_URL="https://api.example.com"

# Public URL of the separately deployed WebSocket server. Use wss:// in production.
NEXT_PUBLIC_WS_BASE_URL="wss://api.example.com"
