FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
COPY prisma ./prisma

# Prisma is a development dependency, so keep all packages in this one-off image.
RUN npm ci

CMD ["npx", "prisma", "db", "push"]
